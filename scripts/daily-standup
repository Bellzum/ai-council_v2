#!/usr/bin/env bash
# Daily Standup CLI wrapper
# Provides hyphenated command name while Python module uses underscores
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec python3 "$SCRIPT_DIR/daily_standup.py" "$@"
