#!/usr/bin/env bash
# Sprint Retrospective CLI wrapper
# Provides hyphenated command name while Python module uses underscores
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec python3 "$SCRIPT_DIR/sprint_retrospective.py" "$@"
